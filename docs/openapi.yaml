openapi: 3.0.0
info:
  title: Mobile Auth API
  version: 1.0.0
  description: Production-grade authentication API for mobile applications.

servers:
  - url: http://localhost:3000
    description: Development server

tags:
  - name: Auth
    description: Authentication and verification endpoints

paths:
  # Pulling individual endpoints from docs/paths/auth.yaml
  /api/auth/register:
    $ref: './paths/auth.yaml#/paths/~1api~1auth~1register'

  /api/auth/login:
    $ref: './paths/auth.yaml#/paths/~1api~1auth~1login'

  /api/auth/verify-otp:
    $ref: './paths/auth.yaml#/paths/~1api~1auth~1verify-otp'